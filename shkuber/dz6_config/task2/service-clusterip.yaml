apiVersion: v1
kind: Service
metadata:
  name: nginx-service-ip
  namespace: netology
spec:
  ports:
  - name: nginx-http
    port: 10080
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-app
  type: ClusterIP